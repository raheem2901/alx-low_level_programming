#include "main.h"
/**
 * _islower - check if the chaacter is lower
 * @c: a chatacter argument
 *  Return: return 1 and 0 depending on condition
 */

int _islower(int c)

{
	return (c >= 'a' && c <= 'z');
}
